Nodester Plans

Multiple instances of backend node processes per app.
Automatically spin up new hosts when current ones are getting loaded.



## Process Logger:

//ps aux | awk '/launch_chroot/ && !/awk/ {print $2,$3,$4,$5,$6,$10,$13}'
