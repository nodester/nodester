#!/bin/bash

export HOME=/var/nodester;
export PATH="/usr/local/bin:${PATH}";

APPDIR=$HOME/nodester;


FHOME=$HOME/forever-proxy


if [ "$@" = ""]; then
    forever list -p $FHOME
else
    forever "$@" -p $FHOME
fi
